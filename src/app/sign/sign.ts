import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-sign',
  imports: [RouterLink],
  templateUrl: './sign.html',
  styleUrl: './sign.css',
})
export class Sign {}
